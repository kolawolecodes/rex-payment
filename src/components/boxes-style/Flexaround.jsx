import { styled } from "@mui/material/styles";

 const Flexaround = styled("div")(({ theme }) => ({
    display: "flex",
    justifyContent: "space-around",
    alignItems: "center"
      
    }));

export default Flexaround